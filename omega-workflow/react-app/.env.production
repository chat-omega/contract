# Production Environment Configuration
# API Base URL - /api prefix for relative paths (nginx proxy forwards to backend)
VITE_API_BASE_URL=/api

# API Timeout (30 seconds)
VITE_API_TIMEOUT=30000

# Environment
VITE_ENV=production
